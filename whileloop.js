myArray = ["The", "Quick", "Brown", "Fox", "Jumps"]

counter = 0;
numberOfElements = myArray.length;
while(counter < numberOfElements) {
    currentElement = myArray[counter];
    console.log(currentElement);
    
    counter++;
}
